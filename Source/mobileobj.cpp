#include "mobileobj.h"

/*void MobileObj::controleMouvements(QKeyEvent *event){
        qDebug("[Controler] move mobile objet ");
            switch (event->key()) {
            case Qt::Key_O:{  // move Forward
                animation.forward();
                break;
            }
            case Qt::Key_L: { //  move Backward
                animation.backward();
                break;
            }
            case Qt::Key_K: {//move Right
                animation.right();
                break;
            }
            case Qt::Key_M: {//move Left
                animation.left();
                break;
            }
            case Qt::Key_I: {//jump
                animation.jump();
            }


    }
    }*/
